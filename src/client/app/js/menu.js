$(document).ready(() => {
    // var appBarEl = document.getElementById('topAppBar');
    // var drawerEl = document.querySelector('.mdc-drawer');
    // var topAppBar = mdc.topAppBar.MDCTopAppBar.attachTo(appBarEl);
    // var drawer = new mdc.drawer.MDCTemporaryDrawer(drawerEl);

    // // Listen for navigation icon events
    // appBarEl.addEventListener('MDCTopAppBar:nav', function () {
    //     drawer.open = true;
    // });
});